<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Productos</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 20px;
    }
    h1 {
      color: #333;
    }
    form {
      margin-bottom: 30px;
      padding: 15px;
      border: 1px solid #ddd;
      background: #f9f9f9;
      border-radius: 5px;
    }
    .producto {
      border: 1px solid #ccc;
      padding: 10px;
      margin-bottom: 10px;
      border-radius: 5px;
    }
    input, textarea {
      display: block;
      margin-bottom: 10px;
      width: 100%;
      padding: 8px;
    }
  </style>
</head>
<body>
  <h1>Crear Producto</h1>
  <form action="/productos" method="POST">
    <input type="text" name="nombre" placeholder="Nombre del producto" required />
    <textarea name="descripcion" placeholder="Descripción"></textarea>
    <input type="text" name="categoria" placeholder="Categoría" required />
    <input type="number" name="precio" placeholder="Precio" step="0.01" required />
    <input type="number" name="stock" placeholder="Stock" required />
    <button type="submit">Guardar</button>
  </form>

  <h1>Lista de Productos</h1>
  <% if (productos.length === 0) { %>
    <p>No hay productos cargados.</p>
  <% } else { %>

    <% productos.forEach(p => { %>
    <div class="producto">
    <h3><%= p.nombre %></h3>
    <p><strong>Descripción:</strong> <%= p.descripcion %></p>
    <p><strong>Categoría:</strong> <%= p.categoria %></p>
    <p><strong>Precio:</strong> $<%= p.precio %></p>
    <p><strong>Stock:</strong> <%= p.stock %></p>

    <!--Botón para eliminar-->
    <span>Formulario de eliminación generado</span>

    <form action="/productos/eliminar/<%= p._id %>" method="POST" onsubmit="return confirm('¿Estás seguro de eliminar este producto?');">
      <button type="submit" style="background-color: red; color: white; border: none; padding: 5px 10px;">Eliminar</button>
    </form>
    </div>

    <!--Boton para editar-->
    <a href="/productos/editar/<%= p._id %>" style="background-color: #007bff; color: white; padding: 5px 10px; text-decoration: none; display: inline-block; margin-top: 5px;">Editar</a>

    
    <% }); %>
  <% } %>
</body>
</html>
